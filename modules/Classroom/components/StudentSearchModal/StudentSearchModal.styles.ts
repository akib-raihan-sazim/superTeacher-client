import { createStyles } from "@mantine/core";

export const useStyles = createStyles(() => ({
  dropdown: {
    overflow: "auto",
  },
  input: {
    zIndex: 1,
  },
  error: {
    marginTop: "0.5rem",
  },
  label: {
    color: "#4CAF50",
    fontWeight: 700,
  },
}));
